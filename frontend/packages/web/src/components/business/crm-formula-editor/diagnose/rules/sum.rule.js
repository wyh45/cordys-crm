const SUM_RULE = {
    name: 'SUM',
    diagnose({ fnNode, args }) {
        const diagnostics = [];
        // 后边SUM的其他规则可扩展在这里
        return diagnostics;
    },
};
export default SUM_RULE;
//# sourceMappingURL=sum.rule.js.map